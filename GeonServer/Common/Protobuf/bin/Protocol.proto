syntax = "proto3";
package Protocol;

import "Enum.proto";
import "Struct.proto";

message C_TEST
{
    uint64 id = 1;
}

message C_MOVE
{
    uint64 id = 1;
    uint32 pos = 2;
}

message S_TEST
{
    uint64 id = 1;
    uint32 hp = 2;
    uint32 attack = 3;
    repeated BuffData buffs = 4;
}

message S_LOGIN
{

}